package com.projeto.task.dto;

public record UserResponseDTO(Long id,
                              String nome,
                              String email,
                              String cargo) {
}
